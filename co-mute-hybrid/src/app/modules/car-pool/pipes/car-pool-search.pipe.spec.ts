import { CarPoolSearchPipe } from './car-pool-search.pipe';

describe('CarPoolSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new CarPoolSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
